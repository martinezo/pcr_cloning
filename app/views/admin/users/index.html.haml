#menu
  =render partial: 'layouts/admin_menu'

%h1= t('titles.users')

=render 'layouts/search', url: admin_users_path

#record-list
  =render partial: 'record_tbl'

#will-paginate
  = will_paginate @admin_users, params: {id: nil, action: 'index'}

%br

= link_to_fa_button({url: new_admin_user_path, text: t('commands.new'), icon: 'fa-plus-square-o'})

#show-dialog
#new-update-dialog
#delete-dialog
