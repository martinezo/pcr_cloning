.error-explanation

= simple_form_for @admin_user, :html => {:novalidate => true}, remote: true do |f|
  = f.input :login, :input_html => {:size => 15}
  = f.input :name, :input_html => {:size => 40}
  = f.input :mail, :input_html => {:size => 40}
  .sp
  %hr
  .t-right
    = jq_submit_tag t('commands.save') 
